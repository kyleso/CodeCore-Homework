const makeMemberArray = (members) => {
  return members.split(", ")
}

const teamPicker = (members, pickMethod) => {
  if (pickMethod === 'teamCount') {

  } else if (pickMethod === 'numberPerTeam') {
    
  }

}

